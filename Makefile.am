cfd_all:
	cd src && make cfd_all

#	cd test && make cfd_all

cfd_clean:
	cd src && make cfd_clean

#	cd test && make cfd_clean

# build for src
SUBDIRS = src

pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = pc/cfd-js.pc
